@model List<SportsRegistration.EF.Sport>

@{
    ViewBag.Title = "Register";
}

@if (TempData["Message"] != null)
{
    <div class="alert alert-success">
        @TempData["Message"]
    </div>
}

<h2>Register</h2>

<form method="post">
    @foreach (var s in Model)
    {
        <label>
            <input type="checkbox" name="sports" value="@s.SId" /> @s.SName (@s.Count)
        </label>
        <br />
    }

    <button type="submit" class="btn btn-primary">Apply Register</button>
    <button type="reset" class="btn btn-secondary">Cancel</button>
</form>
